Demo project for Spring Boot Rest APIs

Run instructions:
1. Copy src/main/resources/application.properties.example to src/main/resources/application.properties and set environment-specific values (do not commit secrets).
2. Build and run with: ./mvnw spring-boot:run

Note: The project now uses Jakarta Bean Validation. POST/PUT endpoints validate incoming payloads.
